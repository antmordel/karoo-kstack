<?xml version="1.0" encoding="utf-8"?>
<!--
  One section per field, built at runtime from Definitions.all so a field added later appears here
  without touching this file.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="16dp">

        <LinearLayout
            android:id="@+id/fields"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />
    </ScrollView>

    <!--
      A Karoo is used with gloves on and the back gesture is not obvious on this screen, so leaving
      needs an unmissable target that stays put while the list scrolls.
    -->
    <Button
        android:id="@+id/close"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:text="@string/main_screen_close" />
</LinearLayout>
