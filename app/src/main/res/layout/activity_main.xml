<?xml version="1.0" encoding="utf-8"?>
<!--
  The whole screen closes on tap, and the button says so. A Karoo is used with gloves on and its
  back gesture is not obvious on this screen, so leaving needs an unmissable target.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clickable="true"
    android:focusable="true"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="24dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="@string/main_screen_body"
        android:textSize="16sp" />

    <Button
        android:id="@+id/close"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:text="@string/main_screen_close" />
</LinearLayout>
